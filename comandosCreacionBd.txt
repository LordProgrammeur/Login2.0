Perri si quiere crear la base de datos en mysql xampp tiene que insertar estos comandos y ya la crea y le tiene que funcionar todo: CREATE TABLE usuarios (
    id INT(6) UNSIGNED AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(30) NOT NULL,
    email VARCHAR(50),
    contraseña VARCHAR(50),
    reg_date TIMESTAMP
);
